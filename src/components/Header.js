import React from 'react';
import { View, Text } from 'react-native';

const Header = (props) => (

        <View>
            <Text>Esse e nosso Header</Text>
        </View>
);

export default Header;